# Financial Analysis Tool - Environment Variables
# Copy this file to .env and customize as needed

# Secret key for session encryption (generate a secure random string for production)
SECRET_KEY=change-me-in-production-use-a-long-random-string

# Flask environment (development/production)
FLASK_ENV=production

# SSL/HTTPS Configuration (handled by NGINX)
# Set to 'true' to enable HTTPS on port 443
# When enabled, HTTP (port 80) automatically redirects to HTTPS
SSL_ENABLED=false

# To use your own certificates, place them in the ./certs directory:
#   ./certs/cert.pem - SSL certificate
#   ./certs/key.pem  - Private key
# If no certificates are found, self-signed certs will be generated automatically
